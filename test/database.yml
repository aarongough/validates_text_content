sqlite:
  :adapter: sqlite
  :dbfile: vendor/plugins/validates_text_content/test/validates_text_content_plugin.sqlite.db

sqlite3:
  :adapter: sqlite3
  :dbfile: vendor/plugins/validates_text_content/test/validates_text_content_plugin.sqlite3.db

postgresql:
  :adapter: postgresql
  :username: postgres
  :password: postgres
  :database: validates_text_content_plugin_test
  :min_messages: ERROR

mysql:
  :adapter: mysql
  :host: localhost
  :username: root
  :password: password
  :database: validates_text_content_plugin_test
